import { beforeEach, describe, test, expect } from 'vitest';
import { screen, render } from '@testing-library/react';
import UtilityContainer from './../UtilityContainer.tsx';

describe('Utility Container component', () => {
    beforeEach(() => {

    });

    test('Utility container component displays', () => {

    });

    test('Utility container has correct width', () => {

    });

    test('Utility container has correct height', () => {

    });

    test('Utility container has correct background-color', () => {

    });

    test('Utility container has correct border attribute', () => {

    });

    test('Each child container has correct alignment (justify-content)', () => {

    });

    test('Backdrop is does not show when backdrop === false', () => {

    });

    test('Backdrop displays when backdrop === true', () => {

    });

    test('Buttons cannot be clicked when backdrop displayed', () => {

    });

    test('Input textboxes cannot be clicked when backdrop displayed', () => {

    });
});